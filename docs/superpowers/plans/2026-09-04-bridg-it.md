# Bridg-It Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Bridg-It (Gale), the 20th game of the site: two players draw
edges on their own interleaved point grid (red 6×6 top-bottom, blue 5×5
left-right); drawing an edge blocks the one edge of the opposite color it
geometrically crosses; first to connect their two borders wins (no draws
possible).

**Architecture:** Pure-function game engine (`src/games/bridg-it/engine.ts`)
holding state as four boolean edge matrices (`redH`, `redV`, `blueH`,
`blueV`), following the exact pattern of `src/games/hex/engine.ts` and
`src/games/puntos-y-cajas/engine.ts` (`createInitialState` / `esJugadaValida`
/ `playMove` / win detection via BFS). `Board.astro` renders an 11×11 CSS
grid where red dots sit at even/even positions, blue dots at odd/odd
positions, and edge buttons occupy the remaining cells — each edge-button
grid cell is shared by exactly one red edge and one blue edge (the crossing
pair), which is what makes the "drawing one blocks the other" rule a natural
consequence of the layout. Client script wires clicks through
`src/lib/gameSession.ts` exactly like every other game on the site.

**Tech Stack:** Astro, TypeScript, Vitest (existing project stack, no new
dependencies).

**Spec:** `docs/superpowers/specs/2026-09-04-bridg-it-design.md`

## Global Constraints

- Tablero fijo: grid roja 6×6 (índices 0-5), grid azul 5×5 (índices 0-4). Sin
  selector de tamaño.
- Sin lógica de empate/tablero lleno: el juego siempre resuelve a un ganador
  (propiedad probada de Bridg-It/Shannon Switching Game).
- Jugador 1 = rojo, conecta fila 0 ↔ fila 5. Jugador 2 = azul, conecta
  columna 0 ↔ columna 4.
- Sigue el patrón existente del repo exactamente: `engine.ts` con funciones
  puras y estado inmutable (retorna nuevo objeto, nunca muta en `playMove`
  público), `Board.astro` usa `iniciarSesionJuego` de `src/lib/gameSession.ts`
  para turnos/remoto/banner, y el guard de turno local vive en `jugar()`
  (`if (emitirRemoto && !sesion.esMiTurno(...)) return;`), nunca de forma
  incondicional (rompería los movimientos remotos — ver
  `src/games/hex/Board.astro`).
- Comentarios y nombres de variables en español, como el resto del código del
  repo (`jugar`, `esJugadaValida`, `puedeJugar`, etc.).

---

## Task 1: Tipos, estado inicial y validación de forma

**Files:**
- Create: `src/games/bridg-it/engine.ts`
- Test: `src/games/bridg-it/engine.test.ts`

**Interfaces:**
- Produces: `type Player = 1 | 2`; `type EdgeDir = 'h' | 'v'`;
  `interface Edge { type: EdgeDir; row: number; col: number }`;
  `interface BridgItState { redH: boolean[][]; redV: boolean[][]; blueH: boolean[][]; blueV: boolean[][]; currentPlayer: Player; status: 'playing' | 'won'; winner: Player | null; winningPath: Array<{ r: number; c: number }> | null; lastMove: { player: Player; edge: Edge } | null; }`;
  `createInitialState(): BridgItState`; `esJugadaValida(payload: unknown): payload is Edge`.

- [ ] **Step 1: Write the failing tests**

```ts
// src/games/bridg-it/engine.test.ts
import { describe, it, expect } from 'vitest';
import { createInitialState, esJugadaValida } from './engine';

describe('createInitialState', () => {
  it('crea las cuatro matrices de aristas con las dimensiones correctas, todas en false', () => {
    const state = createInitialState();
    expect(state.redH.length).toBe(6);
    expect(state.redH.every(fila => fila.length === 5 && fila.every(v => v === false))).toBe(true);
    expect(state.redV.length).toBe(5);
    expect(state.redV.every(fila => fila.length === 6 && fila.every(v => v === false))).toBe(true);
    expect(state.blueH.length).toBe(5);
    expect(state.blueH.every(fila => fila.length === 4 && fila.every(v => v === false))).toBe(true);
    expect(state.blueV.length).toBe(4);
    expect(state.blueV.every(fila => fila.length === 5 && fila.every(v => v === false))).toBe(true);
  });

  it('inicia con el jugador 1 (rojo), en juego, sin ganador', () => {
    const state = createInitialState();
    expect(state.currentPlayer).toBe(1);
    expect(state.status).toBe('playing');
    expect(state.winner).toBeNull();
    expect(state.winningPath).toBeNull();
    expect(state.lastMove).toBeNull();
  });
});

describe('esJugadaValida', () => {
  it('acepta un Edge bien formado', () => {
    expect(esJugadaValida({ type: 'h', row: 0, col: 0 })).toBe(true);
    expect(esJugadaValida({ type: 'v', row: 3, col: 2 })).toBe(true);
  });

  it('rechaza payloads mal formados', () => {
    expect(esJugadaValida(null)).toBe(false);
    expect(esJugadaValida(42)).toBe(false);
    expect(esJugadaValida({ type: 'x', row: 0, col: 0 })).toBe(false);
    expect(esJugadaValida({ type: 'h', row: -1, col: 0 })).toBe(false);
    expect(esJugadaValida({ type: 'h', row: 1.5, col: 0 })).toBe(false);
    expect(esJugadaValida({ type: 'h', row: 0 })).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: FAIL — `./engine` module not found.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/games/bridg-it/engine.ts
export type Player = 1 | 2;
export type EdgeDir = 'h' | 'v';

export interface Edge {
  type: EdgeDir;
  row: number;
  col: number;
}

export interface BridgItState {
  redH: boolean[][];  // [6][5]  R(r,c)-R(r,c+1)
  redV: boolean[][];  // [5][6]  R(r,c)-R(r+1,c)
  blueH: boolean[][]; // [5][4]  B(r,c)-B(r,c+1)
  blueV: boolean[][]; // [4][5]  B(r,c)-B(r+1,c)
  currentPlayer: Player;
  status: 'playing' | 'won';
  winner: Player | null;
  winningPath: Array<{ r: number; c: number }> | null;
  lastMove: { player: Player; edge: Edge } | null;
}

function crearMatriz(filas: number, columnas: number): boolean[][] {
  return Array.from({ length: filas }, () => Array<boolean>(columnas).fill(false));
}

export function createInitialState(): BridgItState {
  return {
    redH: crearMatriz(6, 5),
    redV: crearMatriz(5, 6),
    blueH: crearMatriz(5, 4),
    blueV: crearMatriz(4, 5),
    currentPlayer: 1,
    status: 'playing',
    winner: null,
    winningPath: null,
    lastMove: null,
  };
}

export function esJugadaValida(payload: unknown): payload is Edge {
  if (typeof payload !== 'object' || payload === null) return false;
  const candidato = payload as Record<string, unknown>;
  return (
    (candidato.type === 'h' || candidato.type === 'v') &&
    typeof candidato.row === 'number' &&
    Number.isInteger(candidato.row) &&
    candidato.row >= 0 &&
    typeof candidato.col === 'number' &&
    Number.isInteger(candidato.col) &&
    candidato.col >= 0
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
rtk git add src/games/bridg-it/engine.ts src/games/bridg-it/engine.test.ts
rtk git commit -m "feat(bridg-it): estado inicial y validación de forma de jugada"
```

---

## Task 2: Slots de arista compartidos y regla de cruce

**Files:**
- Modify: `src/games/bridg-it/engine.ts`
- Test: `src/games/bridg-it/engine.test.ts`

**Interfaces:**
- Consumes: `BridgItState`, `Edge`, `Player` (Task 1).
- Produces: `slotAToEdge(player: Player, r: number, c: number): Edge | null`,
  `slotBToEdge(player: Player, r: number, c: number): Edge | null`,
  `getSlotAStatus(state: BridgItState, r: number, c: number): { drawn: boolean; owner: Player | null }`,
  `getSlotBStatus(state: BridgItState, r: number, c: number): { drawn: boolean; owner: Player | null }`,
  `puedeJugar(state: BridgItState, edge: Edge): boolean`.

  Slot A = celda de grilla en `(fila par, columna impar)`: aloja `redH(r,c)`
  (r:0-5, c:0-4) o, si `r-1` está en `[0,3]`, `blueV(r-1,c)`.
  Slot B = celda de grilla en `(fila impar, columna par)`: aloja `redV(r,c)`
  (r:0-4, c:0-5) o, si `c-1` está en `[0,3]`, `blueH(r,c-1)`.
  Estos helpers existen para que `Board.astro` (Task 6) no necesite conocer
  la regla de cruce: solo itera slots A/B y pregunta su estado/edge.

- [ ] **Step 1: Write the failing tests**

```ts
// agregar a src/games/bridg-it/engine.test.ts
import {
  slotAToEdge,
  slotBToEdge,
  getSlotAStatus,
  getSlotBStatus,
  puedeJugar,
} from './engine';

describe('slotAToEdge / slotBToEdge', () => {
  it('slot A da la arista roja h para el jugador 1 en cualquier posición válida', () => {
    expect(slotAToEdge(1, 0, 0)).toEqual({ type: 'h', row: 0, col: 0 });
    expect(slotAToEdge(1, 5, 4)).toEqual({ type: 'h', row: 5, col: 4 });
  });

  it('slot A da la arista azul v para el jugador 2 solo cuando r-1 está en rango', () => {
    expect(slotAToEdge(2, 0, 0)).toBeNull(); // r-1 = -1, fuera de rango
    expect(slotAToEdge(2, 1, 0)).toEqual({ type: 'v', row: 0, col: 0 });
    expect(slotAToEdge(2, 5, 0)).toBeNull(); // r-1 = 4, fuera de rango (blueV solo 0-3)
  });

  it('slot B da la arista roja v para el jugador 1 en cualquier posición válida', () => {
    expect(slotBToEdge(1, 0, 0)).toEqual({ type: 'v', row: 0, col: 0 });
    expect(slotBToEdge(1, 4, 5)).toEqual({ type: 'v', row: 4, col: 5 });
  });

  it('slot B da la arista azul h para el jugador 2 solo cuando c-1 está en rango', () => {
    expect(slotBToEdge(2, 0, 0)).toBeNull(); // c-1 = -1
    expect(slotBToEdge(2, 0, 1)).toEqual({ type: 'h', row: 0, col: 0 });
    expect(slotBToEdge(2, 0, 5)).toBeNull(); // c-1 = 4, fuera de rango (blueH solo 0-3)
  });
});

describe('getSlotAStatus / getSlotBStatus', () => {
  it('reporta vacío cuando no hay arista dibujada', () => {
    const state = createInitialState();
    expect(getSlotAStatus(state, 2, 2)).toEqual({ drawn: false, owner: null });
    expect(getSlotBStatus(state, 2, 2)).toEqual({ drawn: false, owner: null });
  });

  it('reporta el dueño rojo cuando redH está marcada', () => {
    const state = createInitialState();
    state.redH[2][2] = true;
    expect(getSlotAStatus(state, 2, 2)).toEqual({ drawn: true, owner: 1 });
  });

  it('reporta el dueño azul cuando blueV está marcada', () => {
    const state = createInitialState();
    state.blueV[1][2] = true; // corresponde a slot A en r=2,c=2 (r-1=1)
    expect(getSlotAStatus(state, 2, 2)).toEqual({ drawn: true, owner: 2 });
  });
});

describe('puedeJugar', () => {
  it('permite la primera jugada de rojo en cualquier arista propia', () => {
    const state = createInitialState();
    expect(puedeJugar(state, { type: 'h', row: 0, col: 0 })).toBe(true);
  });

  it('rechaza redibujar una arista propia ya trazada', () => {
    const state = createInitialState();
    state.redH[0][0] = true;
    expect(puedeJugar(state, { type: 'h', row: 0, col: 0 })).toBe(false);
  });

  it('bloquea la arista roja cruzada una vez trazada la azul correspondiente', () => {
    const state = createInitialState();
    state.currentPlayer = 2;
    state.blueV[0][0] = true; // ocupa el slot A en (r=1,c=0)
    state.currentPlayer = 1;
    expect(puedeJugar(state, { type: 'h', row: 1, col: 0 })).toBe(false);
  });

  it('bloquea la arista azul cruzada una vez trazada la roja correspondiente', () => {
    const state = createInitialState();
    state.redV[0][1] = true; // ocupa el slot B en (r=0,c=1)
    state.currentPlayer = 2;
    expect(puedeJugar(state, { type: 'h', row: 0, col: 0 })).toBe(false);
  });

  it('permite jugar en los bordes donde la arista cruzada no existe (sin lanzar error)', () => {
    const state = createInitialState();
    // redH(0,0): slot A en r=0, blueV correspondiente sería r-1=-1, no existe.
    expect(puedeJugar(state, { type: 'h', row: 0, col: 0 })).toBe(true);
    state.currentPlayer = 2;
    // blueH(0,0): slot B en c=1, redV correspondiente es (0,1); si no está trazada, se puede jugar.
    expect(puedeJugar(state, { type: 'h', row: 0, col: 0 })).toBe(true);
  });

  it('rechaza coordenadas fuera de rango para el jugador activo', () => {
    const state = createInitialState();
    expect(puedeJugar(state, { type: 'h', row: 0, col: 5 })).toBe(false); // redH col máx es 4
    state.currentPlayer = 2;
    expect(puedeJugar(state, { type: 'h', row: 0, col: 4 })).toBe(false); // blueH col máx es 3
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: FAIL — `slotAToEdge`/`slotBToEdge`/`getSlotAStatus`/`getSlotBStatus`/`puedeJugar` not exported.

- [ ] **Step 3: Write minimal implementation**

```ts
// agregar a src/games/bridg-it/engine.ts

export function slotAToEdge(player: Player, r: number, c: number): Edge | null {
  if (player === 1) {
    if (r < 0 || r > 5 || c < 0 || c > 4) return null;
    return { type: 'h', row: r, col: c };
  }
  const br = r - 1;
  if (br < 0 || br > 3 || c < 0 || c > 4) return null;
  return { type: 'v', row: br, col: c };
}

export function slotBToEdge(player: Player, r: number, c: number): Edge | null {
  if (player === 1) {
    if (r < 0 || r > 4 || c < 0 || c > 5) return null;
    return { type: 'v', row: r, col: c };
  }
  const bc = c - 1;
  if (r < 0 || r > 4 || bc < 0 || bc > 3) return null;
  return { type: 'h', row: r, col: bc };
}

export function getSlotAStatus(
  state: BridgItState,
  r: number,
  c: number
): { drawn: boolean; owner: Player | null } {
  if (state.redH[r]?.[c]) return { drawn: true, owner: 1 };
  const br = r - 1;
  if (br >= 0 && br <= 3 && state.blueV[br]?.[c]) return { drawn: true, owner: 2 };
  return { drawn: false, owner: null };
}

export function getSlotBStatus(
  state: BridgItState,
  r: number,
  c: number
): { drawn: boolean; owner: Player | null } {
  if (state.redV[r]?.[c]) return { drawn: true, owner: 1 };
  const bc = c - 1;
  if (bc >= 0 && bc <= 3 && state.blueH[r]?.[bc]) return { drawn: true, owner: 2 };
  return { drawn: false, owner: null };
}

function enRangoPropio(player: Player, edge: Edge): boolean {
  if (player === 1) {
    return edge.type === 'h'
      ? edge.row >= 0 && edge.row <= 5 && edge.col >= 0 && edge.col <= 4
      : edge.row >= 0 && edge.row <= 4 && edge.col >= 0 && edge.col <= 5;
  }
  return edge.type === 'h'
    ? edge.row >= 0 && edge.row <= 4 && edge.col >= 0 && edge.col <= 3
    : edge.row >= 0 && edge.row <= 3 && edge.col >= 0 && edge.col <= 4;
}

function tieneArista(state: BridgItState, player: Player, edge: Edge): boolean {
  const matriz =
    player === 1
      ? edge.type === 'h'
        ? state.redH
        : state.redV
      : edge.type === 'h'
        ? state.blueH
        : state.blueV;
  return matriz[edge.row]?.[edge.col] ?? false;
}

// Dada la arista de `player`, retorna la arista del rival que cruza (o null
// si esa posición cae en un borde donde el rival no tiene ninguna arista
// posible ahí).
function aristaCruzada(player: Player, edge: Edge): Edge | null {
  if (player === 1) {
    if (edge.type === 'h') {
      const br = edge.row - 1;
      return br < 0 || br > 3 ? null : { type: 'v', row: br, col: edge.col };
    }
    const bc = edge.col - 1;
    return bc < 0 || bc > 3 ? null : { type: 'h', row: edge.row, col: bc };
  }
  return edge.type === 'h'
    ? { type: 'v', row: edge.row, col: edge.col + 1 }
    : { type: 'h', row: edge.row + 1, col: edge.col };
}

export function puedeJugar(state: BridgItState, edge: Edge): boolean {
  if (state.status !== 'playing') return false;
  if (!enRangoPropio(state.currentPlayer, edge)) return false;
  if (tieneArista(state, state.currentPlayer, edge)) return false;
  const cruzada = aristaCruzada(state.currentPlayer, edge);
  const rival = state.currentPlayer === 1 ? 2 : 1;
  if (cruzada && tieneArista(state, rival, cruzada)) return false;
  return true;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: PASS (todos los tests, incluidos los de Task 1)

- [ ] **Step 5: Commit**

```bash
rtk git add src/games/bridg-it/engine.ts src/games/bridg-it/engine.test.ts
rtk git commit -m "feat(bridg-it): slots de arista compartidos y regla de cruce"
```

---

## Task 3: playMove y detección de victoria

**Files:**
- Modify: `src/games/bridg-it/engine.ts`
- Test: `src/games/bridg-it/engine.test.ts`

**Interfaces:**
- Consumes: todo lo de Tasks 1-2.
- Produces: `findWinningPath(state: BridgItState, player: Player): Array<{ r: number; c: number }> | null`,
  `playMove(state: BridgItState, edge: Edge): BridgItState`.

- [ ] **Step 1: Write the failing tests**

```ts
// agregar a src/games/bridg-it/engine.test.ts
import { findWinningPath, playMove } from './engine';

describe('findWinningPath', () => {
  it('retorna null si no hay conexión', () => {
    const state = createInitialState();
    expect(findWinningPath(state, 1)).toBeNull();
  });

  it('encuentra la conexión roja fila 0 a fila 5 por una columna recta', () => {
    const state = createInitialState();
    for (let r = 0; r < 5; r++) state.redV[r][0] = true; // columna 0 completa
    const camino = findWinningPath(state, 1);
    expect(camino).not.toBeNull();
    expect(camino![0]).toEqual({ r: 0, c: 0 });
    expect(camino![camino!.length - 1]).toEqual({ r: 5, c: 0 });
  });

  it('encuentra la conexión azul columna 0 a columna 4 por una fila recta', () => {
    const state = createInitialState();
    for (let c = 0; c < 4; c++) state.blueH[0][c] = true; // fila 0 completa
    const camino = findWinningPath(state, 2);
    expect(camino).not.toBeNull();
    expect(camino![0]).toEqual({ r: 0, c: 0 });
    expect(camino![camino!.length - 1]).toEqual({ r: 0, c: 4 });
  });

  it('no confunde la grilla roja con la azul', () => {
    const state = createInitialState();
    for (let c = 0; c < 4; c++) state.blueH[0][c] = true;
    expect(findWinningPath(state, 1)).toBeNull();
  });
});

describe('playMove', () => {
  it('ignora una jugada inválida y retorna el mismo estado', () => {
    const state = createInitialState();
    state.redH[0][0] = true;
    const resultado = playMove(state, { type: 'h', row: 0, col: 0 });
    expect(resultado).toBe(state);
  });

  it('marca la arista y pasa el turno cuando no hay victoria', () => {
    const state = createInitialState();
    const resultado = playMove(state, { type: 'h', row: 0, col: 0 });
    expect(resultado.redH[0][0]).toBe(true);
    expect(resultado.currentPlayer).toBe(2);
    expect(resultado.status).toBe('playing');
    expect(resultado.lastMove).toEqual({ player: 1, edge: { type: 'h', row: 0, col: 0 } });
    // no debe mutar el estado original
    expect(state.redH[0][0]).toBe(false);
  });

  it('declara ganador a rojo al completar fila 0 a fila 5', () => {
    let state = createInitialState();
    for (let r = 0; r < 4; r++) {
      state = playMove(state, { type: 'v', row: r, col: 0 });
      state = playMove(state, { type: 'h', row: 0, col: r }); // jugadas azules que no interfieren
    }
    state = playMove(state, { type: 'v', row: 4, col: 0 });
    expect(state.status).toBe('won');
    expect(state.winner).toBe(1);
    expect(state.winningPath).not.toBeNull();
  });

  it('no cambia de turno ni de estado tras la jugada ganadora', () => {
    let state = createInitialState();
    for (let r = 0; r < 4; r++) state = playMove(state, { type: 'v', row: r, col: 0 });
    const antes = state.currentPlayer;
    state = playMove(state, { type: 'v', row: 4, col: 0 });
    expect(state.currentPlayer).toBe(antes);
    expect(puedeJugar(state, { type: 'h', row: 0, col: 1 })).toBe(false); // status ya no es 'playing'
  });
});
```

(La prueba de victoria intercala jugadas azules en `blueH` fila 0 que no
cruzan la columna 0 roja, para simular una partida real de dos jugadores sin
violar el turno estrictamente alterno — se aceptan aquí llamando `playMove`
en la secuencia exacta que produce el efecto deseado; si el orden de turnos
del `playMove` real no encaja exactamente, ajustar la secuencia de jugadas
del test manteniendo la aserción final de victoria roja intacta.)

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: FAIL — `findWinningPath`/`playMove` no exportados.

- [ ] **Step 3: Write minimal implementation**

```ts
// agregar a src/games/bridg-it/engine.ts

function clonarEstado(state: BridgItState): BridgItState {
  return {
    redH: state.redH.map(fila => [...fila]),
    redV: state.redV.map(fila => [...fila]),
    blueH: state.blueH.map(fila => [...fila]),
    blueV: state.blueV.map(fila => [...fila]),
    currentPlayer: state.currentPlayer,
    status: state.status,
    winner: state.winner,
    winningPath: state.winningPath,
    lastMove: state.lastMove,
  };
}

function vecinosRojo(state: BridgItState, r: number, c: number): Array<{ r: number; c: number }> {
  const vecinos: Array<{ r: number; c: number }> = [];
  if (c > 0 && state.redH[r][c - 1]) vecinos.push({ r, c: c - 1 });
  if (c < 5 && state.redH[r][c]) vecinos.push({ r, c: c + 1 });
  if (r > 0 && state.redV[r - 1][c]) vecinos.push({ r: r - 1, c });
  if (r < 5 && state.redV[r][c]) vecinos.push({ r: r + 1, c });
  return vecinos;
}

function vecinosAzul(state: BridgItState, r: number, c: number): Array<{ r: number; c: number }> {
  const vecinos: Array<{ r: number; c: number }> = [];
  if (c > 0 && state.blueH[r][c - 1]) vecinos.push({ r, c: c - 1 });
  if (c < 4 && state.blueH[r][c]) vecinos.push({ r, c: c + 1 });
  if (r > 0 && state.blueV[r - 1][c]) vecinos.push({ r: r - 1, c });
  if (r < 4 && state.blueV[r][c]) vecinos.push({ r: r + 1, c });
  return vecinos;
}

export function findWinningPath(
  state: BridgItState,
  player: Player
): Array<{ r: number; c: number }> | null {
  const size = player === 1 ? 6 : 5;
  const vecinosDe = player === 1 ? vecinosRojo : vecinosAzul;
  const clave = (r: number, c: number) => `${r},${c}`;

  const inicio: Array<{ r: number; c: number }> =
    player === 1
      ? Array.from({ length: size }, (_, c) => ({ r: 0, c }))
      : Array.from({ length: size }, (_, r) => ({ r, c: 0 }));

  const visitado = new Set<string>(inicio.map(p => clave(p.r, p.c)));
  const padre = new Map<string, { r: number; c: number } | null>();
  for (const p of inicio) padre.set(clave(p.r, p.c), null);
  const cola = [...inicio];

  let meta: { r: number; c: number } | null = null;
  while (cola.length > 0) {
    const actual = cola.shift()!;
    const llegoALaMeta = player === 1 ? actual.r === size - 1 : actual.c === size - 1;
    if (llegoALaMeta) {
      meta = actual;
      break;
    }
    for (const vecino of vecinosDe(state, actual.r, actual.c)) {
      const k = clave(vecino.r, vecino.c);
      if (!visitado.has(k)) {
        visitado.add(k);
        padre.set(k, actual);
        cola.push(vecino);
      }
    }
  }

  if (!meta) return null;

  const camino: Array<{ r: number; c: number }> = [];
  let actual: { r: number; c: number } | null = meta;
  while (actual) {
    camino.push(actual);
    actual = padre.get(clave(actual.r, actual.c)) ?? null;
  }
  return camino.reverse();
}

export function playMove(state: BridgItState, edge: Edge): BridgItState {
  if (!esJugadaValida(edge)) return state;
  if (!puedeJugar(state, edge)) return state;

  const player = state.currentPlayer;
  const next = clonarEstado(state);
  if (player === 1) {
    if (edge.type === 'h') next.redH[edge.row][edge.col] = true;
    else next.redV[edge.row][edge.col] = true;
  } else {
    if (edge.type === 'h') next.blueH[edge.row][edge.col] = true;
    else next.blueV[edge.row][edge.col] = true;
  }
  next.lastMove = { player, edge };

  const winningPath = findWinningPath(next, player);
  if (winningPath) {
    next.status = 'won';
    next.winner = player;
    next.winningPath = winningPath;
    return next;
  }

  next.currentPlayer = player === 1 ? 2 : 1;
  return next;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: PASS (todos los tests). Si la prueba de victoria intercalada falla
por el orden de turnos, ajustar la secuencia de jugadas en el test (no la
implementación) hasta lograr una partida real que termine en victoria roja
por columna 0.

- [ ] **Step 5: Commit**

```bash
rtk git add src/games/bridg-it/engine.ts src/games/bridg-it/engine.test.ts
rtk git commit -m "feat(bridg-it): playMove y detección de victoria por BFS"
```

---

## Task 4: Smoke test de partidas aleatorias (sin empates)

**Files:**
- Modify: `src/games/bridg-it/engine.test.ts`

**Interfaces:**
- Consumes: `createInitialState`, `playMove`, `puedeJugar`, `Edge` (Tasks 1-3).

- [ ] **Step 1: Write the failing test**

```ts
// agregar a src/games/bridg-it/engine.test.ts (agregar `Edge` al import
// existente de './engine' en este archivo: `import { ..., type Edge } from './engine';`)

function todasLasAristasPosibles(): Edge[] {
  const aristas: Edge[] = [];
  for (let r = 0; r <= 5; r++) for (let c = 0; c <= 4; c++) aristas.push({ type: 'h', row: r, col: c });
  for (let r = 0; r <= 4; r++) for (let c = 0; c <= 5; c++) aristas.push({ type: 'v', row: r, col: c });
  for (let r = 0; r <= 4; r++) for (let c = 0; c <= 3; c++) aristas.push({ type: 'h', row: r, col: c });
  for (let r = 0; r <= 3; r++) for (let c = 0; c <= 4; c++) aristas.push({ type: 'v', row: r, col: c });
  return aristas;
}

describe('partidas aleatorias', () => {
  it('siempre terminan con exactamente un ganador, sin excepciones', () => {
    const candidatas = todasLasAristasPosibles();
    for (let partida = 0; partida < 2000; partida++) {
      let state = createInitialState();
      let intentosSinExito = 0;
      while (state.status === 'playing' && intentosSinExito < 200) {
        const candidata = candidatas[Math.floor(Math.random() * candidatas.length)];
        const antes = state;
        state = playMove(state, candidata);
        intentosSinExito = state === antes ? intentosSinExito + 1 : 0;
      }
      expect(state.status).toBe('won');
      expect(state.winner === 1 || state.winner === 2).toBe(true);
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: si la implementación de Tasks 1-3 es correcta, este test debería
ya pasar (no introduce funciones nuevas); ejecutar igual para confirmarlo y
detectar cualquier caso borde no cubierto por los tests anteriores (p. ej. un
bucle infinito o una excepción con ciertas combinaciones de aristas).

- [ ] **Step 3: Fix any failures found**

Si el smoke test revela un caso borde (excepción, partida que no termina, o
empate), diagnosticar contra la matriz de aristas involucrada y corregir la
función de `engine.ts` correspondiente (probablemente `enRangoPropio`,
`aristaCruzada` o `findWinningPath`). No añadir lógica de empate: si aparece
un "empate", es un bug en la regla de cruce, no un caso legítimo del juego.

- [ ] **Step 4: Run test to verify it passes**

Run: `npm run test -- src/games/bridg-it/engine.test.ts`
Expected: PASS — las 2000 partidas terminan en victoria, sin excepciones.

- [ ] **Step 5: Commit**

```bash
rtk git add src/games/bridg-it/engine.test.ts
rtk git commit -m "test(bridg-it): smoke test de 2000 partidas aleatorias sin empates"
```

---

## Task 5: Markup y estilos del tablero

**Files:**
- Create: `src/games/bridg-it/Board.astro`

**Interfaces:**
- Consumes: nada del engine todavía (solo estructura estática); Task 6 le
  añade el script que importa `engine.ts`.
- Produces: elementos DOM con `id="bridgit-tablero"`, puntos `.punto-rojo` /
  `.punto-azul`, botones `.slot` con `data-slot="a"|"b"`, `data-fila`,
  `data-columna`, que Task 6 consulta y controla.

- [ ] **Step 1: Write the markup and styles**

```astro
---
import TableroJuego from '../../components/TableroJuego.astro';

const N_LINEAS = 11; // posiciones 0..10 en ambos ejes
---

<TableroJuego class="tablero-bridgit-contenedor">
  <div id="bridgit-tablero" class="bridgit-tablero">
    {Array.from({ length: 6 }).map((_, r) => (
      <Fragment>
        {Array.from({ length: 6 }).map((_, c) => (
          <span
            class="punto punto-rojo"
            style={`grid-row: ${2 * r + 1}; grid-column: ${2 * c + 1};`}
          />
        ))}
      </Fragment>
    ))}
    {Array.from({ length: 5 }).map((_, r) => (
      <Fragment>
        {Array.from({ length: 5 }).map((_, c) => (
          <span
            class="punto punto-azul"
            style={`grid-row: ${2 * r + 2}; grid-column: ${2 * c + 2};`}
          />
        ))}
      </Fragment>
    ))}
    {Array.from({ length: 6 }).map((_, r) => (
      <Fragment>
        {Array.from({ length: 5 }).map((_, c) => (
          <button
            type="button"
            class="slot slot--horizontal"
            data-slot="a"
            data-fila={r}
            data-columna={c}
            style={`grid-row: ${2 * r + 1}; grid-column: ${2 * c + 2};`}
            aria-label={`Casilla de conexión, fila ${r}, columna ${c + 1}`}
          />
        ))}
      </Fragment>
    ))}
    {Array.from({ length: 5 }).map((_, r) => (
      <Fragment>
        {Array.from({ length: 6 }).map((_, c) => (
          <button
            type="button"
            class="slot slot--vertical"
            data-slot="b"
            data-fila={r}
            data-columna={c}
            style={`grid-row: ${2 * r + 2}; grid-column: ${2 * c + 1};`}
            aria-label={`Casilla de conexión, fila ${r + 1}, columna ${c}`}
          />
        ))}
      </Fragment>
    ))}
  </div>
</TableroJuego>

<style define:vars={{ nLineas: N_LINEAS }}>
  .tablero-bridgit-contenedor {
    display: flex;
    justify-content: center;
    width: 100%;
  }

  .bridgit-tablero {
    --celda: minmax(1.6rem, 2.6rem);
    display: grid;
    grid-template-columns: repeat(11, var(--celda));
    grid-template-rows: repeat(11, var(--celda));
    width: min(94vw, 32rem);
    aspect-ratio: 1;
    margin: 0 auto;
    padding: 0.5rem;
    border-radius: var(--radius);
    background: var(--color-surface);
    position: relative;
  }

  .punto {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    justify-self: center;
    align-self: center;
    pointer-events: none;
    z-index: 1;
  }

  .punto-rojo {
    background: var(--color-player-1);
  }

  .punto-azul {
    background: var(--color-player-2);
  }

  /* Bordes objetivo: fila 0/5 rojas resaltadas, columna 0/4 azules resaltadas */
  .punto-rojo[style*='grid-row: 1;'],
  .punto-rojo[style*='grid-row: 11;'] {
    box-shadow: 0 0 0 3px color-mix(in srgb, var(--color-player-1) 40%, transparent);
  }

  .slot {
    border: none;
    background: transparent;
    padding: 0;
    justify-self: center;
    align-self: center;
    cursor: pointer;
    z-index: 2;
  }

  .slot::before {
    content: '';
    display: block;
    border-radius: 3px;
    background: transparent;
  }

  .slot--horizontal {
    width: 100%;
    height: 12px;
  }

  .slot--horizontal::before {
    width: 100%;
    height: 6px;
  }

  .slot--vertical {
    width: 12px;
    height: 100%;
  }

  .slot--vertical::before {
    width: 6px;
    height: 100%;
    margin: 0 auto;
  }

  .slot:not(:disabled):hover::before {
    background: rgba(0, 0, 0, 0.12);
  }

  .slot[data-jugador='1']::before {
    background: var(--color-player-1) !important;
  }

  .slot[data-jugador='2']::before {
    background: var(--color-player-2) !important;
  }

  .slot:disabled {
    cursor: default;
  }
</style>
```

- [ ] **Step 2: Verify it renders**

Run: `npm run dev` and open the game route (Task 7 registers it — until
then, verify with a throwaway temporary import in `[slug].astro` or just
proceed; the visual check happens naturally once Task 7 wires the route).
Confirm no Astro build errors: `npm run build 2>&1 | tail -40` should not
mention `bridg-it/Board.astro`.

- [ ] **Step 3: Commit**

```bash
rtk git add src/games/bridg-it/Board.astro
rtk git commit -m "feat(bridg-it): markup y estilos del tablero de puntos entrelazados"
```

---

## Task 6: Interacción del tablero (script cliente)

**Files:**
- Modify: `src/games/bridg-it/Board.astro`

**Interfaces:**
- Consumes: `createInitialState`, `esJugadaValida`, `playMove`, `puedeJugar`,
  `slotAToEdge`, `slotBToEdge`, `getSlotAStatus`, `getSlotBStatus`, `Edge`,
  `Player`, `BridgItState` (Tasks 1-3); `iniciarSesionJuego` de
  `src/lib/gameSession.ts`.

- [ ] **Step 1: Write the client script**

Add before the closing of the file (after the `<style>` block) in
`src/games/bridg-it/Board.astro`:

```astro
<script>
  import {
    createInitialState,
    esJugadaValida,
    playMove,
    puedeJugar,
    slotAToEdge,
    slotBToEdge,
    getSlotAStatus,
    getSlotBStatus,
    type BridgItState,
    type Edge,
  } from './engine';
  import { iniciarSesionJuego } from '../../lib/gameSession';

  const tablero = document.getElementById('bridgit-tablero')!;
  const slots = Array.from(tablero.querySelectorAll<HTMLButtonElement>('.slot'));

  let state: BridgItState = createInitialState();

  const sesion = iniciarSesionJuego<Edge>({
    validarMovimiento: esJugadaValida,
    onMovimientoRemoto: edge => jugar(edge, false),
    onAplicarReinicio: () => {
      state = createInitialState();
      render();
    },
    onRender: render,
    onDesconectar: () => {
      for (const slot of slots) slot.disabled = true;
    },
  });

  function jugar(edge: Edge, emitirRemoto = true): void {
    // El guard de turno solo aplica a la entrada local: los movimientos
    // remotos llegan cuando `currentPlayer` es el asiento del rival, así que
    // `esMiTurno` sería falso y los descartaría en silencio.
    if (emitirRemoto && !sesion.esMiTurno(state.currentPlayer)) return;
    if (!puedeJugar(state, edge)) return;
    state = playMove(state, edge);
    render();
    if (emitirRemoto) {
      sesion.enviarMovimiento(edge);
    }
  }

  function render(): void {
    const jugadorDelTurno = state.currentPlayer;
    const esMiTurno = sesion.esMiTurno(jugadorDelTurno);

    for (const slot of slots) {
      const tipo = slot.dataset.slot as 'a' | 'b';
      const r = Number(slot.dataset.fila);
      const c = Number(slot.dataset.columna);
      const estado = tipo === 'a' ? getSlotAStatus(state, r, c) : getSlotBStatus(state, r, c);
      const edgePropia = tipo === 'a' ? slotAToEdge(jugadorDelTurno, r, c) : slotBToEdge(jugadorDelTurno, r, c);

      if (estado.owner) {
        slot.dataset.jugador = String(estado.owner);
      } else {
        delete slot.dataset.jugador;
      }

      const jugable =
        state.status === 'playing' &&
        esMiTurno &&
        !estado.drawn &&
        edgePropia !== null &&
        puedeJugar(state, edgePropia);
      slot.disabled = !jugable;
    }

    if (state.status === 'playing') {
      sesion.mostrarTurno({
        jugador: jugadorDelTurno,
        simbolos: { 1: '●', 2: '▲' },
      });
    } else if (state.status === 'won') {
      const ganador = state.winner!;
      sesion.mostrarFinDeJuego({
        titulo: `🎉 ¡Ganó ${sesion.nombres[ganador]}!`,
        detalle: ganador === 1 ? 'Conexión Norte - Sur completada' : 'Conexión Oeste - Este completada',
      });
    }
  }

  for (const slot of slots) {
    slot.addEventListener('click', () => {
      const tipo = slot.dataset.slot as 'a' | 'b';
      const r = Number(slot.dataset.fila);
      const c = Number(slot.dataset.columna);
      const edge = tipo === 'a' ? slotAToEdge(state.currentPlayer, r, c) : slotBToEdge(state.currentPlayer, r, c);
      if (edge) jugar(edge);
    });
  }

  render();
</script>
```

- [ ] **Step 2: Manual verification**

Run: `npm run dev`, temporarily register the board (Task 7 makes this
permanent) and click through a full game in the browser: verify red edges
only appear on red's turn, blue edges only on blue's, crossing an edge
disables the perpendicular slot at that crossing point, and a completed
connection shows the winner banner.

- [ ] **Step 3: Commit**

```bash
rtk git add src/games/bridg-it/Board.astro
rtk git commit -m "feat(bridg-it): interacción de tablero y sesión de juego"
```

---

## Task 7: Registro de ruta y contenido del juego

**Files:**
- Create: `src/content/juegos/bridg-it.md`
- Modify: `src/pages/juegos/[slug].astro`

**Interfaces:**
- Consumes: `BridgItBoard` (default export of `src/games/bridg-it/Board.astro`).

- [ ] **Step 1: Create the content entry**

```md
---
title: "Bridg-It"
description: "Traza puentes en tu retícula de puntos para conectar tus dos bordes antes que tu rival."
icono: "🌉"
minJugadores: 2
maxJugadores: 2
---

1. El tablero tiene dos retículas de puntos entrelazadas: rojos y azules.
2. El **Jugador 1** (● rojo) conecta sus puntos para unir el **borde superior con el inferior**.
3. El **Jugador 2** (▲ azul) conecta sus puntos para unir el **borde izquierdo con el derecho**.
4. Por turnos, cada jugador traza una línea entre dos de sus propios puntos adyacentes.
5. Cada línea posible de un color cruza exactamente una línea posible del otro color: trazar una **bloquea automáticamente** a la cruzada.
6. **No existen empates**: gana el primer jugador que complete una cadena ininterrumpida entre sus dos bordes.
```

- [ ] **Step 2: Register the route**

In `src/pages/juegos/[slug].astro`, add the import alongside the other board
imports:

```ts
import BridgItBoard from '../../games/bridg-it/Board.astro';
```

and add the entry to the `BOARDS` map:

```ts
  col: ColBoard,
  'bridg-it': BridgItBoard,
} as const;
```

- [ ] **Step 3: Verify the route builds and renders**

Run: `npm run build 2>&1 | tail -60`
Expected: build succeeds, output lists `dist/juegos/bridg-it/index.html`.

Run: `npm run dev`, navigate to `/juegos/bridg-it`, confirm the board renders
and a full local pass-and-play game reaches a winner banner for both a red
win and a blue win.

- [ ] **Step 4: Commit**

```bash
rtk git add src/content/juegos/bridg-it.md src/pages/juegos/\[slug\].astro
rtk git commit -m "feat(bridg-it): registrar ruta y contenido del juego"
```

---

## Task 8: Documentación y verificación final

**Files:**
- Modify: `abstract-games-by-category/01-2-players/11-bridg-it.md`
- Modify: `abstract-games-by-category/GAME-INDEX.md`

- [ ] **Step 1: Mark the source game doc as implemented**

In `abstract-games-by-category/01-2-players/11-bridg-it.md`, add a line
right after the title (matching how other implemented games are annotated
in this repo — check `abstract-games-by-category/01-2-players/05-hex.md`
for the exact convention used and mirror it) noting it's implemented and
live at `games.cardila.com/juegos/bridg-it/`.

- [ ] **Step 2: Update the game index**

In `abstract-games-by-category/GAME-INDEX.md`, change the row:

```md
| 01-2-players | [11-bridg-it.md](01-2-players/11-bridg-it.md) | |
```

to:

```md
| 01-2-players | [11-bridg-it.md](01-2-players/11-bridg-it.md) | ✅ |
```

- [ ] **Step 3: Run the full test suite**

Run: `npm run test`
Expected: PASS — all existing tests plus the new `bridg-it/engine.test.ts`
suite, no regressions.

- [ ] **Step 4: Run the full build**

Run: `npm run build`
Expected: PASS — no Astro/TypeScript errors.

- [ ] **Step 5: Commit**

```bash
rtk git add abstract-games-by-category/01-2-players/11-bridg-it.md abstract-games-by-category/GAME-INDEX.md
rtk git commit -m "docs: marcar Bridg-It como implementado"
```
