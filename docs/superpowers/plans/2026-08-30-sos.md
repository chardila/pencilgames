# SOS Game Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the game SOS as a new 2-player paper-and-pencil game with a 6×6 grid, letter selector, declarative SVG line tracing, local/remote multiplayer, and scoring with bonus turns.

**Architecture:** Pure immutable TypeScript game engine (`engine.ts`) tested with Vitest (`engine.test.ts`), coupled with an Astro UI component (`Board.astro`) using `<TableroJuego>` and `iniciarSesionJuego` for local/WebRTC remote play, registered in Astro content collections and dynamic routes.

**Tech Stack:** TypeScript, Astro, Vitest, WebRTC / `gameSession.ts`, SVG, CSS Grid.

**Spec:** [docs/superpowers/specs/2026-08-30-sos-design.md](file:///home/carlos-ardila/Documents/gitprojects/pencilgames/docs/superpowers/specs/2026-08-30-sos-design.md)

## Global Constraints

- **Board Size**: Fixed 6×6 grid (36 cells).
- **Players**: 2 players (Player 1: orange `●`, Player 2: blue `▲`).
- **Letters**: `'S'` and `'O'`.
- **Scoring**: +1 point per completed `S-O-S` straight line (horizontal, vertical, diagonal). Multiple SOS formed in a single move are counted.
- **Turn Bonus**: Completing at least 1 SOS allows the current player to repeat their turn.
- **Game Over**: Grid full (36 cells filled). Highest score wins, or draw if scores are equal.
- **No External Dependencies**: Use native TypeScript and existing `src/lib/gameSession.ts` infrastructure.
- **Always prefix terminal commands with `rtk`** as per `CLAUDE.md`.

---

### Task 1: SOS Game Engine & Tests (`engine.ts` + `engine.test.ts`)

**Files:**
- Create: `src/games/sos/engine.ts`
- Create: `src/games/sos/engine.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export type Player = 1 | 2;
  export type Letter = 'S' | 'O';
  export type CellValue = Letter | null;
  export type GameStatus = 'playing' | 'finished';

  export interface Point {
    row: number;
    col: number;
  }

  export interface SOSLine {
    from: Point;
    to: Point;
    player: Player;
  }

  export interface Move {
    row: number;
    col: number;
    letter: Letter;
  }

  export interface SOSState {
    size: number;
    board: CellValue[][];
    completedLines: SOSLine[];
    currentPlayer: Player;
    scores: Record<Player, number>;
    status: GameStatus;
    winner: Player | 'draw' | null;
    lastMove: Move | null;
  }

  export function createInitialState(size?: number): SOSState;
  export function esJugadaValida(payload: unknown): payload is Move;
  export function playMove(state: SOSState, move: Move): SOSState;
  ```

- [ ] **Step 1: Write failing engine tests**

Create `src/games/sos/engine.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import {
  createInitialState,
  esJugadaValida,
  playMove,
  type SOSState,
  type Move,
} from './engine';

describe('SOS Engine', () => {
  describe('createInitialState', () => {
    it('creates a 6x6 empty board by default', () => {
      const state = createInitialState();
      expect(state.size).toBe(6);
      expect(state.board).toHaveLength(6);
      state.board.forEach(row => {
        expect(row).toHaveLength(6);
        expect(row.every(cell => cell === null)).toBe(true);
      });
      expect(state.currentPlayer).toBe(1);
      expect(state.scores).toEqual({ 1: 0, 2: 0 });
      expect(state.completedLines).toEqual([]);
      expect(state.status).toBe('playing');
      expect(state.winner).toBeNull();
      expect(state.lastMove).toBeNull();
    });
  });

  describe('esJugadaValida', () => {
    it('accepts valid move payloads', () => {
      expect(esJugadaValida({ row: 0, col: 0, letter: 'S' })).toBe(true);
      expect(esJugadaValida({ row: 5, col: 5, letter: 'O' })).toBe(true);
    });

    it('rejects invalid coordinates and letters', () => {
      expect(esJugadaValida(null)).toBe(false);
      expect(esJugadaValida(42)).toBe(false);
      expect(esJugadaValida('S')).toBe(false);
      expect(esJugadaValida({ row: -1, col: 0, letter: 'S' })).toBe(false);
      expect(esJugadaValida({ row: 6, col: 0, letter: 'S' })).toBe(false);
      expect(esJugadaValida({ row: 0, col: 6, letter: 'S' })).toBe(false);
      expect(esJugadaValida({ row: 1.5, col: 0, letter: 'S' })).toBe(false);
      expect(esJugadaValida({ row: 0, col: 0, letter: 'X' })).toBe(false);
      expect(esJugadaValida({ row: 0, col: 0 })).toBe(false);
    });
  });

  describe('playMove basic flow', () => {
    it('places a letter and alternates turn if no SOS is formed', () => {
      const s0 = createInitialState(6);
      const s1 = playMove(s0, { row: 0, col: 0, letter: 'S' });

      expect(s1.board[0][0]).toBe('S');
      expect(s1.currentPlayer).toBe(2);
      expect(s1.scores).toEqual({ 1: 0, 2: 0 });
      expect(s1.completedLines).toHaveLength(0);
      expect(s1.lastMove).toEqual({ row: 0, col: 0, letter: 'S' });
    });

    it('ignores move on occupied cell', () => {
      const s0 = createInitialState(6);
      const s1 = playMove(s0, { row: 0, col: 0, letter: 'S' });
      const s2 = playMove(s1, { row: 0, col: 0, letter: 'O' });

      expect(s2).toBe(s1);
      expect(s2.board[0][0]).toBe('S');
      expect(s2.currentPlayer).toBe(2);
    });

    it('ignores move if game status is finished', () => {
      const state: SOSState = {
        ...createInitialState(6),
        status: 'finished',
      };
      const next = playMove(state, { row: 0, col: 0, letter: 'S' });
      expect(next).toBe(state);
    });
  });

  describe('SOS Detection when placing O', () => {
    it('detects horizontal S-O-S and grants bonus turn', () => {
      let state = createInitialState(6);
      // P1 places S at (0,0)
      state = playMove(state, { row: 0, col: 0, letter: 'S' });
      // P2 places S at (0,2)
      state = playMove(state, { row: 0, col: 2, letter: 'S' });
      // P1 places O at (0,1) -> forms S-O-S!
      state = playMove(state, { row: 0, col: 1, letter: 'O' });

      expect(state.scores[1]).toBe(1);
      expect(state.scores[2]).toBe(0);
      expect(state.completedLines).toEqual([
        { from: { row: 0, col: 0 }, to: { row: 0, col: 2 }, player: 1 },
      ]);
      // P1 keeps the turn
      expect(state.currentPlayer).toBe(1);
    });

    it('detects vertical S-O-S', () => {
      let state = createInitialState(6);
      state = playMove(state, { row: 0, col: 0, letter: 'S' }); // P1
      state = playMove(state, { row: 2, col: 0, letter: 'S' }); // P2
      state = playMove(state, { row: 1, col: 0, letter: 'O' }); // P1 -> forms vertical SOS

      expect(state.scores[1]).toBe(1);
      expect(state.completedLines).toEqual([
        { from: { row: 0, col: 0 }, to: { row: 2, col: 0 }, player: 1 },
      ]);
      expect(state.currentPlayer).toBe(1);
    });

    it('detects diagonal ↘ and ↗ S-O-S', () => {
      let state = createInitialState(6);
      state = playMove(state, { row: 0, col: 0, letter: 'S' }); // P1
      state = playMove(state, { row: 2, col: 2, letter: 'S' }); // P2
      state = playMove(state, { row: 1, col: 1, letter: 'O' }); // P1 -> diagonal ↘

      expect(state.scores[1]).toBe(1);
      expect(state.completedLines).toEqual([
        { from: { row: 0, col: 0 }, to: { row: 2, col: 2 }, player: 1 },
      ]);
    });

    it('detects multiple simultaneous SOS with a single O', () => {
      let state = createInitialState(6);
      // Place S in a cross and diagonals around (1,1)
      state = playMove(state, { row: 1, col: 0, letter: 'S' }); // P1
      state = playMove(state, { row: 1, col: 2, letter: 'S' }); // P2
      state = playMove(state, { row: 0, col: 1, letter: 'S' }); // P1
      state = playMove(state, { row: 2, col: 1, letter: 'S' }); // P2
      // P1 places O at (1,1) -> completes both horizontal and vertical SOS (2 points!)
      state = playMove(state, { row: 1, col: 1, letter: 'O' }); // P1

      expect(state.scores[1]).toBe(2);
      expect(state.completedLines).toHaveLength(2);
      expect(state.currentPlayer).toBe(1);
    });
  });

  describe('SOS Detection when placing S', () => {
    it('detects S-O-S when placing the ending S', () => {
      let state = createInitialState(6);
      state = playMove(state, { row: 0, col: 0, letter: 'S' }); // P1
      state = playMove(state, { row: 0, col: 1, letter: 'O' }); // P2
      state = playMove(state, { row: 0, col: 2, letter: 'S' }); // P1 -> forms SOS!

      expect(state.scores[1]).toBe(1);
      expect(state.completedLines).toEqual([
        { from: { row: 0, col: 0 }, to: { row: 0, col: 2 }, player: 1 },
      ]);
      expect(state.currentPlayer).toBe(1);
    });

    it('detects S-O-S when placing the starting S', () => {
      let state = createInitialState(6);
      state = playMove(state, { row: 0, col: 2, letter: 'S' }); // P1
      state = playMove(state, { row: 0, col: 1, letter: 'O' }); // P2
      state = playMove(state, { row: 0, col: 0, letter: 'S' }); // P1 -> forms SOS!

      expect(state.scores[1]).toBe(1);
      expect(state.completedLines).toEqual([
        { from: { row: 0, col: 0 }, to: { row: 0, col: 2 }, player: 1 },
      ]);
      expect(state.currentPlayer).toBe(1);
    });
  });

  describe('Grid limits and no wrap-around', () => {
    it('does not detect false SOS across row boundaries', () => {
      let state = createInitialState(6);
      // Row 0 end: (0, 5)='S', Row 1 start: (1, 0)='O', (1, 1)='S'
      state = playMove(state, { row: 0, col: 5, letter: 'S' }); // P1
      state = playMove(state, { row: 1, col: 0, letter: 'O' }); // P2
      state = playMove(state, { row: 1, col: 1, letter: 'S' }); // P1
      expect(state.completedLines).toHaveLength(0);
      expect(state.scores).toEqual({ 1: 0, 2: 0 });
    });
  });

  describe('Game completion and winner', () => {
    it('declares finished and computes winner when board is full', () => {
      // 2x2 custom board for test simplicity
      let state = createInitialState(2);
      state = playMove(state, { row: 0, col: 0, letter: 'S' });
      state = playMove(state, { row: 0, col: 1, letter: 'O' });
      state = playMove(state, { row: 1, col: 0, letter: 'S' });
      state = playMove(state, { row: 1, col: 1, letter: 'O' });

      expect(state.status).toBe('finished');
      expect(state.winner).toBe('draw');
    });
  });
});
```

- [ ] **Step 2: Run Vitest to verify tests fail**

Run: `rtk vitest run src/games/sos/engine.test.ts`
Expected: FAIL (Cannot find module `./engine`)

- [ ] **Step 3: Implement `src/games/sos/engine.ts`**

Write `src/games/sos/engine.ts`:
```ts
export type Player = 1 | 2;
export type Letter = 'S' | 'O';
export type CellValue = Letter | null;
export type GameStatus = 'playing' | 'finished';

export interface Point {
  row: number;
  col: number;
}

export interface SOSLine {
  from: Point;
  to: Point;
  player: Player;
}

export interface Move {
  row: number;
  col: number;
  letter: Letter;
}

export interface SOSState {
  size: number;
  board: CellValue[][];
  completedLines: SOSLine[];
  currentPlayer: Player;
  scores: Record<Player, number>;
  status: GameStatus;
  winner: Player | 'draw' | null;
  lastMove: Move | null;
}

export function esJugadaValida(payload: unknown): payload is Move {
  if (typeof payload !== 'object' || payload === null) return false;
  const c = payload as Record<string, unknown>;
  return (
    typeof c.row === 'number' &&
    Number.isInteger(c.row) &&
    c.row >= 0 &&
    c.row < 6 &&
    typeof c.col === 'number' &&
    Number.isInteger(c.col) &&
    c.col >= 0 &&
    c.col < 6 &&
    (c.letter === 'S' || c.letter === 'O')
  );
}

export function createInitialState(size = 6): SOSState {
  const board: CellValue[][] = Array.from({ length: size }, () =>
    Array(size).fill(null)
  );
  return {
    size,
    board,
    completedLines: [],
    currentPlayer: 1,
    scores: { 1: 0, 2: 0 },
    status: 'playing',
    winner: null,
    lastMove: null,
  };
}

function normalizeLine(p1: Point, p2: Point, player: Player): SOSLine {
  if (p1.row < p2.row || (p1.row === p2.row && p1.col <= p2.col)) {
    return { from: p1, to: p2, player };
  }
  return { from: p2, to: p1, player };
}

function areLinesEqual(l1: SOSLine, l2: SOSLine): boolean {
  return (
    l1.from.row === l2.from.row &&
    l1.from.col === l2.from.col &&
    l1.to.row === l2.to.row &&
    l1.to.col === l2.to.col
  );
}

function findNewLines(
  board: CellValue[][],
  size: number,
  move: Move,
  player: Player
): SOSLine[] {
  const newLines: SOSLine[] = [];
  const { row: r, col: c, letter } = move;

  const inBounds = (row: number, col: number) =>
    row >= 0 && row < size && col >= 0 && col < size;

  if (letter === 'O') {
    // Check 4 axes centered at (r, c)
    const axes = [
      { dr: 0, dc: 1 },  // Horizontal
      { dr: 1, dc: 0 },  // Vertical
      { dr: 1, dc: 1 },  // Diagonal ↘
      { dr: 1, dc: -1 }, // Diagonal ↗
    ];

    for (const { dr, dc } of axes) {
      const r1 = r - dr, c1 = c - dc;
      const r2 = r + dr, c2 = c + dc;
      if (inBounds(r1, c1) && inBounds(r2, c2)) {
        if (board[r1][c1] === 'S' && board[r2][c2] === 'S') {
          const line = normalizeLine({ row: r1, col: c1 }, { row: r2, col: c2 }, player);
          if (!newLines.some(l => areLinesEqual(l, line))) {
            newLines.push(line);
          }
        }
      }
    }
  } else if (letter === 'S') {
    // Check all 8 directions from (r, c)
    const directions = [
      { dr: 0, dc: 1 },
      { dr: 0, dc: -1 },
      { dr: 1, dc: 0 },
      { dr: -1, dc: 0 },
      { dr: 1, dc: 1 },
      { dr: 1, dc: -1 },
      { dr: -1, dc: 1 },
      { dr: -1, dc: -1 },
    ];

    for (const { dr, dc } of directions) {
      const ro = r + dr, co = c + dc;
      const rs = r + 2 * dr, cs = c + 2 * dc;
      if (inBounds(ro, co) && inBounds(rs, cs)) {
        if (board[ro][co] === 'O' && board[rs][cs] === 'S') {
          const line = normalizeLine({ row: r, col: c }, { row: rs, col: cs }, player);
          if (!newLines.some(l => areLinesEqual(l, line))) {
            newLines.push(line);
          }
        }
      }
    }
  }

  return newLines;
}

export function playMove(state: SOSState, move: Move): SOSState {
  if (state.status !== 'playing') return state;
  const { row, col, letter } = move;

  if (row < 0 || row >= state.size || col < 0 || col >= state.size) return state;
  if (state.board[row][col] !== null) return state;

  const nextBoard = state.board.map(r => [...r]);
  nextBoard[row][col] = letter;

  const newLines = findNewLines(nextBoard, state.size, move, state.currentPlayer);
  const nextCompletedLines = [...state.completedLines, ...newLines];
  const nextScores = { ...state.scores };
  nextScores[state.currentPlayer] += newLines.length;

  const isFull = nextBoard.every(r => r.every(cell => cell !== null));

  let nextStatus: GameStatus = 'playing';
  let nextWinner: Player | 'draw' | null = null;
  let nextPlayer: Player = state.currentPlayer;

  if (isFull) {
    nextStatus = 'finished';
    if (nextScores[1] > nextScores[2]) {
      nextWinner = 1;
    } else if (nextScores[2] > nextScores[1]) {
      nextWinner = 2;
    } else {
      nextWinner = 'draw';
    }
  } else {
    // If completed at least 1 SOS, player retains turn; otherwise toggle
    nextPlayer = newLines.length > 0 ? state.currentPlayer : state.currentPlayer === 1 ? 2 : 1;
  }

  return {
    size: state.size,
    board: nextBoard,
    completedLines: nextCompletedLines,
    currentPlayer: nextPlayer,
    scores: nextScores,
    status: nextStatus,
    winner: nextWinner,
    lastMove: move,
  };
}
```

- [ ] **Step 4: Run Vitest to verify all tests pass**

Run: `rtk vitest run src/games/sos/engine.test.ts`
Expected: PASS (all tests green)

- [ ] **Step 5: Commit engine and tests**

Run:
```bash
rtk git add src/games/sos/engine.ts src/games/sos/engine.test.ts
rtk git commit -m "feat(sos): agregar motor logico puro y tests TDD"
```

---

### Task 2: Content Collection Entry (`src/content/juegos/sos.md`)

**Files:**
- Create: `src/content/juegos/sos.md`

- [ ] **Step 1: Create `src/content/juegos/sos.md`**

Write `src/content/juegos/sos.md`:
```md
---
title: "SOS"
description: "Coloca letras S u O en una cuadrícula de 6×6 y suma puntos completando secuencias S-O-S."
icono: "🆘"
minJugadores: 2
maxJugadores: 2
---

1. El juego se disputa sobre un tablero de 6×6 casillas.
2. En cada turno, el jugador activo elige colocar una **S** o una **O** en cualquier casilla vacía.
3. Si la letra colocada completa una o más secuencias **S-O-S** en línea recta (horizontal, vertical o diagonal):
   - El jugador suma **1 punto** por cada SOS formado.
   - Se traza una línea del color del jugador sobre las tres letras.
   - El jugador **vuelve a jugar** (mantiene el turno).
4. Si la jugada no forma ningún SOS, el turno pasa al rival.
5. La partida finaliza cuando el tablero queda completamente lleno (36 casillas). Gana quien haya obtenido más puntos; si empatan en puntaje, es un empate.
```

- [ ] **Step 2: Commit content file**

Run:
```bash
rtk git add src/content/juegos/sos.md
rtk git commit -m "docs(sos): agregar ficha de contenido e instrucciones"
```

---

### Task 3: Board Component (`src/games/sos/Board.astro`)

**Files:**
- Create: `src/games/sos/Board.astro`

**Interfaces:**
- Consumes: `engine.ts` (`createInitialState`, `esJugadaValida`, `playMove`, `SOSState`, `Move`, `Letter`), `src/lib/gameSession.ts` (`iniciarSesionJuego`), `src/components/TableroJuego.astro`.

- [ ] **Step 1: Implement `src/games/sos/Board.astro`**

Write `src/games/sos/Board.astro`:
```astro
---
import TableroJuego from '../../components/TableroJuego.astro';

const SIZE = 6;
---

<TableroJuego class="tablero-sos">
  <div class="selector-letra" role="radiogroup" aria-label="Elegir letra">
    <span class="selector-letra__label">Letra:</span>
    <div class="selector-letra__controles">
      <button
        type="button"
        role="radio"
        aria-checked="true"
        class="btn-letra activo"
        data-letra="S"
        id="btn-letra-s"
      >
        S
      </button>
      <button
        type="button"
        role="radio"
        aria-checked="false"
        class="btn-letra"
        data-letra="O"
        id="btn-letra-o"
      >
        O
      </button>
    </div>
  </div>

  <div class="tablero-sos__contenedor">
    <svg id="lineas-sos-svg" class="tablero-sos__lineas" viewBox="0 0 600 600" aria-hidden="true"></svg>

    <div id="tablero-grid" class="tablero-sos__grid" role="grid" aria-label="Tablero SOS de 6 por 6">
      {Array.from({ length: SIZE }).map((_, r) =>
        Array.from({ length: SIZE }).map((_, c) => (
          <button
            type="button"
            class="casilla"
            data-fila={r}
            data-columna={c}
            aria-label={`Fila ${r + 1}, Columna ${c + 1}`}
          />
        ))
      )}
    </div>
  </div>
</TableroJuego>

<style>
  .tablero-sos {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .selector-letra {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: color-mix(in srgb, var(--color-surface) 80%, rgba(0, 0, 0, 0.04));
    padding: 0.35rem 0.75rem;
    border-radius: 9999px;
    border: 1px solid rgba(0, 0, 0, 0.08);
  }

  .selector-letra__label {
    font-size: 0.9rem;
    font-weight: 700;
    opacity: 0.8;
  }

  .selector-letra__controles {
    display: flex;
    gap: 0.35rem;
  }

  .btn-letra {
    min-width: 2.5rem;
    min-height: 2.2rem;
    font-size: 1.1rem;
    font-weight: 800;
    border: 2px solid transparent;
    border-radius: 9999px;
    background: transparent;
    cursor: pointer;
    color: var(--color-text);
    transition: all 0.15s ease;
  }

  .btn-letra:hover:not(:disabled) {
    background: rgba(0, 0, 0, 0.05);
  }

  .btn-letra.activo {
    background: var(--color-text);
    color: var(--color-surface);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  }

  .btn-letra:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .tablero-sos__contenedor {
    position: relative;
    width: min(92vw, 26rem);
    aspect-ratio: 1;
    margin: 0 auto;
  }

  .tablero-sos__lineas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 2;
  }

  .tablero-sos__lineas :global(.linea-sos) {
    stroke-linecap: round;
    stroke-width: 7;
    animation: drawLine 0.25s ease-out;
  }

  .tablero-sos__lineas :global(.linea-sos--p1) {
    stroke: var(--color-player-1);
    filter: drop-shadow(0 1px 2px color-mix(in srgb, var(--color-player-1) 50%, transparent));
  }

  .tablero-sos__lineas :global(.linea-sos--p2) {
    stroke: var(--color-player-2);
    filter: drop-shadow(0 1px 2px color-mix(in srgb, var(--color-player-2) 50%, transparent));
  }

  @keyframes drawLine {
    from {
      stroke-dasharray: 600;
      stroke-dashoffset: 600;
    }
    to {
      stroke-dasharray: 600;
      stroke-dashoffset: 0;
    }
  }

  .tablero-sos__grid {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    grid-template-rows: repeat(6, 1fr);
    width: 100%;
    height: 100%;
    gap: 4px;
    background: rgba(0, 0, 0, 0.08);
    padding: 4px;
    border-radius: var(--radius);
  }

  .casilla {
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-surface);
    border: none;
    border-radius: 4px;
    font-size: clamp(1.1rem, 4vw, 1.4rem);
    font-weight: 800;
    color: var(--color-text);
    cursor: pointer;
    user-select: none;
    transition: background 0.15s ease, transform 0.1s ease;
    padding: 0;
  }

  .casilla:hover:not(:disabled) {
    background: color-mix(in srgb, var(--color-surface) 90%, black);
    transform: scale(0.98);
  }

  .casilla:disabled {
    cursor: default;
  }

  .casilla.casilla--ultima {
    box-shadow: inset 0 0 0 3px color-mix(in srgb, var(--color-text) 30%, transparent);
  }
</style>

<script>
  import {
    createInitialState,
    esJugadaValida,
    playMove,
    type SOSState,
    type Move,
    type Letter,
  } from './engine';
  import { iniciarSesionJuego } from '../../lib/gameSession';

  const SIZE = 6;
  const SVG_NS = 'http://www.w3.org/2000/svg';

  const tableroGrid = document.getElementById('tablero-grid')!;
  const svgLineas = document.getElementById('lineas-sos-svg') as unknown as SVGSVGElement;
  const casillas = Array.from(tableroGrid.querySelectorAll<HTMLButtonElement>('.casilla'));
  const botonesLetra = Array.from(document.querySelectorAll<HTMLButtonElement>('.btn-letra'));

  let state: SOSState = createInitialState(SIZE);
  let letraActiva: Letter = 'S';
  let repiteTurno = false;

  const sesion = iniciarSesionJuego<Move>({
    validarMovimiento: esJugadaValida,
    onMovimientoRemoto: move => jugar(move, false),
    onAplicarReinicio: () => {
      state = createInitialState(SIZE);
      letraActiva = 'S';
      repiteTurno = false;
      render();
    },
    onRender: render,
    onDesconectar: () => {
      for (const casilla of casillas) casilla.disabled = true;
      for (const btn of botonesLetra) btn.disabled = true;
    },
  });

  function render(): void {
    const esMiTurno = sesion.esMiTurno(state.currentPlayer);

    // Actualizar botones de letra
    for (const btn of botonesLetra) {
      const letra = btn.dataset.letra as Letter;
      const activa = letra === letraActiva;
      btn.classList.toggle('activo', activa);
      btn.setAttribute('aria-checked', String(activa));
      btn.disabled = state.status !== 'playing' || !esMiTurno;
    }

    // Actualizar casillas
    for (const casilla of casillas) {
      const fila = Number(casilla.dataset.fila);
      const columna = Number(casilla.dataset.columna);
      const valor = state.board[fila][columna];

      casilla.textContent = valor ?? '';
      casilla.disabled = valor !== null || state.status !== 'playing' || !esMiTurno;

      const esUltima =
        state.lastMove !== null &&
        state.lastMove.row === fila &&
        state.lastMove.col === columna;
      casilla.classList.toggle('casilla--ultima', esUltima);
    }

    // Renderizar líneas SVG
    svgLineas.innerHTML = '';
    const cellSize = 600 / SIZE;
    for (const line of state.completedLines) {
      const lineEl = document.createElementNS(SVG_NS, 'line');
      const x1 = line.from.col * cellSize + cellSize / 2;
      const y1 = line.from.row * cellSize + cellSize / 2;
      const x2 = line.to.col * cellSize + cellSize / 2;
      const y2 = line.to.row * cellSize + cellSize / 2;

      lineEl.setAttribute('x1', String(x1));
      lineEl.setAttribute('y1', String(y1));
      lineEl.setAttribute('x2', String(x2));
      lineEl.setAttribute('y2', String(y2));
      lineEl.setAttribute('class', `linea-sos linea-sos--p${line.player}`);
      svgLineas.appendChild(lineEl);
    }

    // Actualizar turno / fin de juego
    if (state.status === 'playing') {
      sesion.mostrarTurno({
        jugador: state.currentPlayer,
        etiqueta: sesion.nombres[state.currentPlayer],
        repiteTurno,
        motivoRepeticion: '✨ ¡SOS completado! Vuelves a jugar',
        marcador: {
          1: { nombre: sesion.nombres[1], puntaje: state.scores[1] },
          2: { nombre: sesion.nombres[2], puntaje: state.scores[2] },
        },
      });
    } else {
      let titulo = '🤝 ¡Empate!';
      if (state.winner === 1 || state.winner === 2) {
        titulo = `🎉 ¡Ganó ${sesion.nombres[state.winner]}!`;
      }
      sesion.mostrarFinDeJuego({
        titulo,
        detalle: `${sesion.nombres[1]} ${state.scores[1]} · ${sesion.nombres[2]} ${state.scores[2]}`,
      });
    }
  }

  function jugar(move: Move, emitirRemoto = true): void {
    const prevLinesCount = state.completedLines.length;
    state = playMove(state, move);
    const newLinesCount = state.completedLines.length - prevLinesCount;
    repiteTurno = newLinesCount > 0 && state.status === 'playing';

    render();

    if (emitirRemoto) {
      sesion.enviarMovimiento(move);
    }
  }

  // Event Listeners
  for (const btn of botonesLetra) {
    btn.addEventListener('click', () => {
      letraActiva = btn.dataset.letra as Letter;
      render();
    });
  }

  for (const casilla of casillas) {
    casilla.addEventListener('click', () => {
      const fila = Number(casilla.dataset.fila);
      const columna = Number(casilla.dataset.columna);
      jugar({ row: fila, col: columna, letter: letraActiva });
    });
  }

  // Teclado: teclas S y O cambian letra activa
  document.addEventListener('keydown', e => {
    if (e.key === 's' || e.key === 'S') {
      letraActiva = 'S';
      render();
    } else if (e.key === 'o' || e.key === 'O') {
      letraActiva = 'O';
      render();
    }
  });

  render();
</script>
```

- [ ] **Step 2: Commit Board component**

Run:
```bash
rtk git add src/games/sos/Board.astro
rtk git commit -m "feat(sos): agregar componente Board.astro con selector de letra y SVG"
```

---

### Task 4: Dynamic Route Registration & Catalogue Integration

**Files:**
- Modify: `src/pages/juegos/[slug].astro`

- [ ] **Step 1: Register SOS Board in `src/pages/juegos/[slug].astro`**

Import `SOSBoard` and add `'sos': SOSBoard` to `BOARDS` map in `src/pages/juegos/[slug].astro`.

- [ ] **Step 2: Run type check and tests**

Run: `rtk tsc` (or `npx astro check`)
Run: `rtk vitest run`
Expected: All type checks and tests pass with 0 errors.

- [ ] **Step 3: Test production build**

Run: `npm run build`
Expected: Build succeeds and emits `/juegos/sos`.

- [ ] **Step 4: Commit route integration**

Run:
```bash
rtk git add src/pages/juegos/[slug].astro
rtk git commit -m "feat(sos): registrar juego sos en el mapa de rutas y catálogo"
```

---

### Task 5: Backlog & Verification

**Files:**
- Modify: `abstract-games-by-category/01-2-players/16-sos.md` (if needed)
- Modify: `abstract-games-by-category/GAME-INDEX.md` (mark as implemented)

- [ ] **Step 1: Update backlog index**

Update `abstract-games-by-category/GAME-INDEX.md` marking SOS as implemented (`✅`).

- [ ] **Step 2: Run full verification suite**

Run: `rtk vitest run`
Run: `npx astro check`
Run: `npm run build`

- [ ] **Step 3: Commit final updates**

Run:
```bash
rtk git add abstract-games-by-category/GAME-INDEX.md abstract-games-by-category/01-2-players/16-sos.md
rtk git commit -m "docs: actualizar estado de implementacion de SOS en el catalogo"
```
